# tuples are ordered but they are immutable, means they cannot be changed once created.
# they are similar to list, but immutable, and lists are mutable

a = (1, 3, 2, 2, 6, 4)
print(a)
print(a[1]) # can print the idx number.

# why to use & when to use : 
# we use the tuple when we dont want the value to be accidently changed.

# how do you create tuple with just one element.

num = (1)
print(num)